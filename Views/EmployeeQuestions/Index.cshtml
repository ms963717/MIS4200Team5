@model IEnumerable<MIS4200Team5.Models.EmployeeQuestions>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EmployeeQuestionsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EmployeeQuestionsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EmployeeQuestionsID })
        </td>
    </tr>
}

</table>
